# Env vars (no subir .env real)
# URL del proyecto Supabase (solo si quieres sincronización opcional)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Ajustes de seguridad (cliente)
# Iteraciones/memoria Argon2id podrán calibrarse dinámicamente en runtime
VITE_ARGON2_MEM_MB=128
VITE_ARGON2_ITER=3
